---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`${className} scroll-m-20 w-full mx-auto container md:max-w-2xl lg:max-w-4xl`}
>
  <slot />
</section>
